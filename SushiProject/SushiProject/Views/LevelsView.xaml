<UserControl x:Class="SushiProject.Views.LevelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SushiProject.ViewModels">
    <UserControl.DataContext>
        <vm:LevelsListViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style x:Key="itemstyle" TargetType="{x:Type ListBoxItem}">
            <EventSetter Event="MouseDoubleClick" Handler="HandleDoubleClick" />
        </Style>
    </UserControl.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock ="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Height="64" Width="64" Content="New" />
            <Button Height="64" Width="64" Content="Open" />
            <Button Height="64" Width="64" Content="Rename" />
            <Button Height="64" Width="64" Content="Delete" />
        </StackPanel>
        <ListBox ItemsSource="{Binding LevelCollection}" ItemContainerStyle="{StaticResource itemstyle}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel Width="64" Height="64">
                        <TextBlock DockPanel.Dock="Bottom"
                            Text="{Binding Path=Name}"
                            HorizontalAlignment="Center"/>
                        <Image Source="pack://application:,,,/Assets/level.png" />
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>
